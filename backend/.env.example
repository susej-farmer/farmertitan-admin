# =============================================================================
# FarmerTitan Backend - Environment Configuration
# =============================================================================
#
# IMPORTANT: After running "supabase start", update the Supabase keys below
# with the values from the command output.
#
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (REQUIRED - Update these!)
# -----------------------------------------------------------------------------
# After running "supabase start", copy the keys from the output:
#
# 1. Copy "API URL" -> paste into SUPABASE_URL
# 2. Copy "Publishable key" -> paste into SUPABASE_ANON_KEY
# 3. Copy "Secret key" -> paste into SUPABASE_SERVICE_KEY
#
# Example output from "supabase start":
#          API URL: http://127.0.0.1:54321
#  Publishable key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
#       Secret key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Default/Fallback Supabase Configuration
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=your_publishable_key_here
SUPABASE_SERVICE_KEY=your_secret_key_here

# -----------------------------------------------------------------------------
# Multi-Environment Configuration (Optional - Advanced)
# -----------------------------------------------------------------------------
# You can configure separate databases for local, development, and production.
# The API will use the environment specified in the X-Environment header.
# If not specified, it defaults to 'local'.
#
# Valid X-Environment header values: local, development, production

# LOCAL Environment (Default)
SUPABASE_URL_LOCAL=http://localhost:54321
SUPABASE_ANON_KEY_LOCAL=your_local_anon_key_here
SUPABASE_SERVICE_KEY_LOCAL=your_local_service_key_here

# DEVELOPMENT Environment
SUPABASE_URL_DEV=https://your-dev-project.supabase.co
SUPABASE_ANON_KEY_DEV=your_dev_anon_key_here
SUPABASE_SERVICE_KEY_DEV=your_dev_service_key_here

# PRODUCTION Environment
SUPABASE_URL_PROD=https://your-prod-project.supabase.co
SUPABASE_ANON_KEY_PROD=your_prod_anon_key_here
SUPABASE_SERVICE_KEY_PROD=your_prod_service_key_here

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

PORT=3000
NODE_ENV=development

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------

JWT_SECRET=farmertitan-admin-secret-key-2024
FRONTEND_URL=http://localhost:5174